--- a/apps/browser/src/tools/popup/settings/about-page/about-page-v2.component.html
+++ b/apps/browser/src/tools/popup/settings/about-page/about-page-v2.component.html
@@ -1 +1,0 @@
-<!-- Official Settings overlay source; generated. -->
@@ -3,5 +2 @@
-  <popup-header slot="header" pageTitle="{{ 'about' | i18n }}" showBackButton>
-    <ng-container slot="end">
-      <app-pop-out></app-pop-out>
-    </ng-container>
-  </popup-header>
+  <popup-header slot="header" [pageTitle]="'i18nAbout' | i18n" showBackButton [backAction]="backAction" />
@@ -11,2 +6,8 @@
-      <button type="button" bit-item-content class="about-metadata-row macos-preference-row macos-hit-target" (click)="troubleshoot()">
-        {{ "troubleshooting" | i18n }}
+      <button
+        type="button"
+        bit-item-content
+        class="about-metadata-row macos-preference-row macos-hit-target"
+        (click)="metadataViewChange.emit('troubleshooting')"
+        (keydown)="activateMetadataFromKeyboard($event)"
+      >
+        {{ "i18nTroubleshooting" | i18n }}
@@ -16,2 +17,8 @@
-      <button type="button" bit-item-content class="about-metadata-row macos-preference-row macos-hit-target" (click)="about()">
-        {{ "aboutBitwarden" | i18n }}
+      <button
+        type="button"
+        bit-item-content
+        class="about-metadata-row macos-preference-row macos-hit-target"
+        (click)="metadataViewChange.emit('about')"
+        (keydown)="activateMetadataFromKeyboard($event)"
+      >
+        {{ "i18nAboutProduct" | i18n: metadata.productName }}
@@ -21 +28,11 @@
-      <button type="button" bit-item-content class="about-metadata-row macos-preference-row macos-hit-target" (click)="launchHelp()">
+      <button
+        type="button"
+        bit-item-content
+        class="about-metadata-row macos-preference-row macos-hit-target"
+        (click)="openThirdPartyNotices.emit()"
+      >
+        {{ "i18nThirdPartyOpenSourceLicenses" | i18n }}
+      </button>
+    </bit-item>
+    <bit-item>
+      <button type="button" bit-item-content class="about-metadata-row macos-preference-row macos-hit-target" (click)="openHelp.emit()">
@@ -27,2 +44,2 @@
-      <button type="button" bit-item-content class="about-metadata-row macos-preference-row macos-hit-target" (click)="openWebVault()">
-        {{ "bitWebVaultApp" | i18n }}
+      <button type="button" bit-item-content class="about-metadata-row macos-preference-row macos-hit-target" (click)="openWebVault.emit()">
+        Web Vault
@@ -31,0 +49,11 @@
+    <bit-item>
+      <button
+        type="button"
+        bit-item-content
+        class="about-metadata-row macos-preference-row macos-hit-target"
+        (click)="openProjectSource.emit()"
+      >
+        {{ "i18nProjectSource" | i18n }}
+        <i slot="end" class="bwi bwi-external-link" aria-hidden="true"></i>
+      </button>
+    </bit-item>
@@ -32,0 +61,27 @@
+
+  <p class="about-version-summary">{{ metadata.productName }} {{ metadata.appVersion }}</p>
+
+  <bw-app-update-card
+    [currentVersion]="metadata.appVersion"
+    [view]="updateView"
+    (checkForUpdates)="checkForUpdates.emit()"
+    (downloadAndRestart)="downloadAndRestart.emit()"
+    (deferUpdate)="dismissUpdate.emit()"
+    (retry)="retryUpdate.emit()"
+  />
+
+  <bw-official-about-dialog
+    [metadata]="metadata"
+    [view]="metadataView"
+    [revisionCopyStatus]="revisionCopyStatus"
+    (close)="metadataViewChange.emit(null)"
+    (copyRevision)="copyRevision.emit()"
+  />
+
+  @if (handoffError) {
+    <bw-macos-alert-strip
+      kind="danger"
+      [title]="'i18nUnableToOpenLinkTitle' | i18n"
+      [message]="handoffError"
+    />
+  }
