/**
 * Runtime target for the upstream Utils module's value import of a type-only KeyService.
 * Login view models never resolve a KeyService through this boundary.
 */
export abstract class KeyService {}
